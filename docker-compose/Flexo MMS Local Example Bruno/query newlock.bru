meta {
  name: query newlock
  type: http
  seq: 16
}

post {
  url: {{host}}/orgs/openmbee/repos/test/locks/newlock/query
  body: sparql
  auth: inherit
}

headers {
  Content-Type: application/sparql-query
}

body:sparql {
  select * where {?s ?p ?o .}
}

settings {
  encodeUrl: true
  timeout: 0
}
